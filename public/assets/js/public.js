document.addEventListener('DOMContentLoaded',()=>{const header=document.querySelector('[data-site-header]'),button=document.querySelector('[data-menu-button]'),nav=document.querySelector('[data-site-nav]');const close=()=>{nav?.classList.remove('open');button?.setAttribute('aria-expanded','false');document.body.classList.remove('menu-open')};button?.addEventListener('click',()=>{const open=!nav.classList.contains('open');nav.classList.toggle('open',open);button.setAttribute('aria-expanded',String(open));document.body.classList.toggle('menu-open',open)});nav?.querySelectorAll('a').forEach(a=>a.addEventListener('click',close));document.addEventListener('keydown',e=>{if(e.key==='Escape')close()});const scroll=()=>header?.classList.toggle('scrolled',window.scrollY>12);scroll();window.addEventListener('scroll',scroll,{passive:true});document.querySelectorAll('[data-analytics-event]').forEach(el=>el.addEventListener('click',()=>{if(document.cookie.indexOf('les_sens_consent_analytics=1')<0)return;const token=document.querySelector('meta[name="csrf-token"]')?.content;fetch('/analytics/event',{method:'POST',headers:{'Content-Type':'application/json','X-CSRF-Token':token||''},body:JSON.stringify({event_name:el.dataset.analyticsEvent,event_data:{label:el.textContent.trim().slice(0,80)},page_path:location.pathname})}).catch(()=>{})}))});
